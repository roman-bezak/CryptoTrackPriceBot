name: Release

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      # Клонируем репозиторий полностью
      - uses: actions/checkout@v3

      # Настройка Node.js (опционально)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18"

      # Парсим CHANGELOG.md для текущего тега
      - name: Extract release notes
        id: changelog
        run: |
          TAG=${GITHUB_REF#refs/tags/}
          echo "🔍 Ищем заметки для тега: ${TAG}"
          
          # Проверяем существование файла
          if [ ! -f "CHANGELOG.md" ]; then
            echo "❌ Файл CHANGELOG.md не найден!"
            exit 1
          fi
          
          echo "📖 Содержимое CHANGELOG.md (первые 15 строк):"
          head -15 CHANGELOG.md
          
          echo ""
          echo "🔍 Ищем секцию для тега [${TAG}]..."
          
          # Убираем 'v' из начала тега для поиска в CHANGELOG.md
          TAG_WITHOUT_V=${TAG#v}
          echo "🔍 Ищем без 'v': [${TAG_WITHOUT_V}]"
          
          # Ищем секцию с точным совпадением тега (без 'v')
          NOTES=$(awk "/## \\[${TAG_WITHOUT_V}\\]/,/^## /{if(!/^## /) print}" CHANGELOG.md | sed '/^\s*$/d')
          
          if [ -z "$NOTES" ]; then
            echo "⚠️ Заметки не найдены для тега [${TAG_WITHOUT_V}]"
            echo "🔍 Попробуем найти с 'v': [${TAG}]"
            NOTES=$(awk "/## \\[${TAG}\\]/,/^## /{if(!/^## /) print}" CHANGELOG.md | sed '/^\s*$/d')
            
            if [ -z "$NOTES" ]; then
              echo "❌ Заметки все еще не найдены"
              echo "📝 Создаем fallback заметки..."
              NOTES="## ${TAG}\n\nРелиз создан автоматически. Заметки о релизе не найдены в CHANGELOG.md."
            else
              echo "✅ Заметки найдены с 'v' в начале"
            fi
          else
            echo "✅ Заметки найдены без 'v' в начале"
          fi
          
          echo ""
          echo "📝 Извлеченные заметки:"
          echo "$NOTES"
          echo ""
          
          echo "notes<<EOF" >> $GITHUB_ENV
          echo "$NOTES" >> $GITHUB_ENV
          echo "EOF" >> $GITHUB_ENV
          
          echo "✅ Заметки сохранены в переменную окружения"

      # Создаем релиз
      - name: Create GitHub Release
        run: |
          echo "🏷️ Создаем релиз для тега: ${{ github.ref_name }}"
          echo "📝 Заметки из переменной окружения:"
          echo "${{ env.notes }}"
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body: ${{ env.notes }}
          files: ""  # пусто → только исходники
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
